In order to run you need to install the libraries necessary for the RetinaNet.
Please, see https://github.com/fizyr/keras-retinanet.

To run the code, just do:

./run.sh

Important: fix the path inside run.sh (experiment_path=/dataset_path/dataset/) 
